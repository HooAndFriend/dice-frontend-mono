// ** Component Imports
import TicketHeader from "../TicketHeader";
import TicketItem from "../TicketItem";

const TicketTable = () => {
  return (
    <div>
      <TicketHeader />
      <TicketItem />
      <TicketItem />
      <TicketItem />
    </div>
  );
};

export default TicketTable;
