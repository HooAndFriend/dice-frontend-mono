// ** Component Imports
import CustomSearchView from "./custom-search";

interface PropsType {}

const CustomSearch = ({}: PropsType) => {
  return <CustomSearchView />;
};

export default CustomSearch;
